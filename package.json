{
  "name": "egauge-data-collection",
  "version": "1.0.0",
  "description": "This application is intended to serve as an interface to collect that data from an eGauge data logger.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc ",
    "dev": "npm run compile && node ./dist/src/index.js",
    "build-dev": "npx tsc && mkdir ./dist/env/ &&cp ./env/.shared.env ./dist/env/.shared.env"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.9",
    "@types/node-cron": "^3.0.6",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.1.0",
    "dotenv": "^16.0.3",
    "node-cron": "^3.0.2"
  }
}
